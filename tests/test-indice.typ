#import "../lib.typ": *

#set page(paper: "a4", margin: 2cm, numbering: "1")
#set text(size: 12pt, lang: "pt", region: "BR")

= Capítulo 1

A #irem("derivada") derivada é um conceito fundamental do #irem("cálculo"). A #irem("derivada", sub: "parcial") derivada parcial generaliza o conceito para funções de várias variáveis.

O conceito de #irem("limite") limite é a base do cálculo.

#pagebreak()

= Capítulo 2

A #irem("integral") integral é a operação inversa da #irem("derivada") derivada.

A #irem("integral", sub: "definida") integral definida calcula a área sob a curva.
A #irem("integral", sub: "imprópria") integral imprópria estende o conceito para intervalos infinitos.

O #irem("teorema fundamental do cálculo") teorema fundamental conecta derivadas e integrais.

#irem-def("continuidade") é a propriedade de uma função sem saltos.

#irem("velocidade", ver: "cinemática")

#pagebreak()

= Capítulo 3

O #irem("cálculo") cálculo é utilizado em toda a física.

A #irem("derivada", sub: "direcional") derivada direcional mede a taxa de variação em uma direção específica.

#irem("função") Funções são o objeto central da análise.

#pagebreak()

#imprimir-indice()
